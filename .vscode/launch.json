{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI (Docker)",
      "type": "python",
      "request": "launch",
      "program": "uvicorn",
      "args": ["app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"],
      "justMyCode": true
    }
  ]
}
