pandas>=2.0.0
pydantic>=2.0.0
fastapi>=0.110.0
uvicorn>=0.21.0
numpy>=1.26.0

# Let op: voor de container / volledige pipeline worden dependencies beheerd via Poetry in
# pyproject.toml. Dit bestand blijft voor snelle lokale playground tests minimaal.
